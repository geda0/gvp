# Optional: copy to repo root as `.env` for `docker compose` overrides (gitignored).
# Local stack defaults: CHAT_PROVIDER=mock needs no API keys.

# Chat service (docker compose `chat` service — pass via env_file or environment:)
CHAT_PROVIDER=mock
# CHAT_PROVIDER=gemini
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.0-flash
# CHAT_PROVIDER=openai
# OPENAI_API_KEY=
