# Plan: Pre-prod hardening → stage

> Ordered ONE-behavior TDD queue from the P0/P1/P2 backlog + ADR-0008/0009.
> Layer key: `[app]` node:test (js/, aws/src cores) · `[app-infra]` aws/template.yaml
> (config + manual review, no node seam) · `[chat]` pytest (docker/chat) · `[fe-manual]`
> browser/qa-verified · `[CI]` workflow/yml. GATED = touches a SECURITY_GLOB path
> (aws/template.yaml, events-ingress*.js, common/{resend,report-queries*}.js, common/
> {contact,events}-shared.js per the glob, docker/chat/app/main.py) → run under
> `SECURITY_REVIEW=1`; the ADR is the cleared review.

## P0 — ship-blockers for the stage deploy

- [x] S1 [app] hashIp gains a pepper arg → `hashIp(ip, pepper)` returns HMAC-SHA256(pepper, ip) sliced to 16 hex, differing from the unkeyed SHA-256 for the same IP, and same (ip,pepper) → same hash (inv: SEC-2 ipHash-keyed) — target: NEW `test/contact-shared-iphash.test.mjs` asserting the keyed value ≠ legacy hash + determinism. GATED: needs SECURITY_REVIEW=1 (cleared by ADR-0008; common/contact-shared.js). Pure helper — no env. First (fastest red→green, unblocks S2/S3).
- [x] S2 [app] empty/missing pepper fails safe → `hashIp(ip, '')` throws (or returns the documented non-IP fallback key, never the unkeyed legacy hash) (inv: SEC-2 fail-safe) — target: `test/contact-shared-iphash.test.mjs` asserts the chosen fail-closed behavior (navigator note: prefer throw). GATED: SECURITY_REVIEW=1 (ADR-0008). Depends S1.
- [x] S3 [app] events ingress threads the pepper through → `buildEventRecord`/`normalizeEventBatch` hash the client IP with the injected `IP_HASH_PEPPER` from env (same IP within an epoch → same ipHash → uniqueVisitors still dedups to 1) (inv: SEC-2 metric preserved) — target: NEW `test/events-iphash-pepper.test.mjs` (or extend events-shared.test) asserting two events from one IP+pepper share an ipHash and a different pepper changes it. GATED: SECURITY_REVIEW=1 (ADR-0008; events-shared.js). Depends S1/S2.
- [x] S4 [app] consent predicate gates the beacon → with no stored consent flag `flushEvents()` buffers but emits NO post (sendBeacon/fetch not called); after the flag is set `flushEvents()` posts (inv: SEC-1 default-deny beacon) — target: NEW `test/consent-gate.test.mjs` stubbing localStorage + navigator.sendBeacon/fetch on `js/site-events.js` + a `js/consent.js` `hasAnalyticsConsent()` seam. Not gated. Parallel-safe with S1–S3 (disjoint files).
- [x] S5 [app] consent predicate gates GA → with no consent flag `initAnalytics()` does NOT call `gtag('config', …)`; after consent it configures GA as today (inv: SEC-1 default-deny GA) — target: `test/consent-gate.test.mjs` stubbing window.gtag/dataLayer on `js/analytics.js`. Not gated. Depends S4 (shares `js/consent.js`); serialize with S4.
- [x] S6 [fe-manual] consent banner UX → first visit shows a dismissible, theme-consistent, keyboard-dismissible, reduced-motion-respecting banner; accept persists across reloads and starts GA+beacon; decline/dismiss persists suppression and re-shows nothing (inv: SEC-1 UX) — verify: qa-verifier in a browser per design-notes Decision 2. Not gated. Depends S4/S5 (the gate they pinned is the banner's switch).
- [x] S7 [app-infra] DailyReportFunction Errors alarm → `aws/template.yaml` declares an `AWS/Lambda` `Errors` alarm (FunctionName dimension, Threshold 1, Sum/300s, GreaterThanOrEqualToThreshold, TreatMissingData notBreaching) wired to `ContactAlarmTopic`; same alarm for `ContactFailureReportFunction` (inv: INFRA-2 observable) — verify: review against ADR-0009 §INFRA-2 vs the `ChatLambdaErrorsAlarm` pattern (no node seam; config + manual). GATED: SECURITY_REVIEW=1 (ADR-0009; aws/template.yaml). Parallel-safe with S1–S6 (template only).

## P1 — this milestone (before stage deploy)

- [x] S8 [app] AGG-3/TC-04 test asserts the real nested property → the empty-day test asserts `report.chat.avgFirstTokenMs === undefined` and a deliberate NaN there fails it (inv: AGG-3 non-vacuous) — target: fix `test/daily-report.test.mjs:85` (`report.avgFirstTokenMs` → `report.chat.avgFirstTokenMs`). Not gated. Test-quality only; code already correct — do first in P1 (free red→green).
- [x] S9 [app] avgFirstToken has one canonical definition → the daily digest's stream+ok avg is the shared definition; a regression that diverges the admin-panel "all finite turns" number from it goes red (inv: AGG-2 single source) — target: NEW/extended `test/daily-report.test.mjs` pinning the stream+ok aggregator as canonical (reuse `aggregateChat`). Not gated. Parallel-safe with S8 if disjoint test files; else serialize on daily-report.test.mjs.
- [x] S10 [app] EV-3 202 count is unambiguous → the events 202 body reports `persisted` (or adds `received`/`dropped`) so a dropped/over-cap event is not silently folded into a bare `accepted` (inv: EV-3) — target: extend `test/events-ingress-core.test.mjs` asserting the renamed/extended count on a batch where rows are dropped. GATED: SECURITY_REVIEW=1 (ADR? — events-ingress-core.js is in the glob; no ADR clears EV-3 → flag for navigator, see notes). Depends ordering on S12 (shares events-ingress-core.js) — serialize.
- [x] S11 [app] EV-2 daily-report send is idempotent per date → the Resend send carries a stable `Idempotency-Key` keyed on the report date, so two sends for the same day resolve to one delivered email (inv: EV-2) — target: NEW `test/resend-idempotency.test.mjs` (or extend) asserting `sendViaResend` forwards an `Idempotency-Key` header derived from the report date. GATED: SECURITY_REVIEW=1 (ADR? — common/resend.js is in the glob; not cleared by ADR-0008/0009 → flag for navigator, see notes). Parallel-safe (resend.js disjoint).
- [x] S12 [app] events ingress caps body size before parse → `parseJsonBody` (or the events handler) rejects a decoded body over ~64KB with 400/413 BEFORE `JSON.parse` runs; a ≤40-event batch is accepted unchanged (inv: EV-4/SEC-4/INFRA-5 pre-parse cap) — target: extend `test/events-ingress-core.test.mjs` + a contact-shared parse test asserting the oversize rejection and the central guard hardens both routes. GATED: SECURITY_REVIEW=1 (events-ingress-core.js + common/contact-shared.js in glob; not directly cleared by ADR-0008/0009 — but ADR-0009 backlog lists SEC-4 → flag for navigator, see notes). Serialize with S10 (same file).
- [x] S13 [app] MAX_EVENTS_PER_BATCH 100 → 25 → a 40-event batch normalizes to ≤25 rows (one BatchWrite of fan-out) (inv: SEC-3 amplification bounded) — target: extend `test/events-shared.test.mjs` asserting the lowered cap. GATED: SECURITY_REVIEW=1 (cleared by ADR-0009; the single constant in events-shared.js). Serialize with S3 (same file) — land S3 first.
- [x] S14 [app] FE-3 storage-disabled visitors get distinct session ids → when `sessionStorage` throws, `getSessionId()` returns a fresh per-load random id (two such visitors differ), and the stable per-tab id is unchanged when storage works (inv: FE-3) — target: NEW `test/site-events-session.test.mjs` stubbing a throwing sessionStorage on `js/site-events.js`. Not gated. Serialize with S4 (same file js/site-events.js).
- [x] S15 [app] FE-4 every contact_submit has a terminal outcome → a 2xx response with a missing/non-object body or non-JSON content-type emits `contact_submit_error` (e.g. `reason:'unexpected_reply'`) before returning; success and 4xx/5xx/validation/network paths still emit their existing terminal events (inv: FE-4 funnel closed) — target: NEW `test/contact-funnel.test.mjs` (extract the submit-outcome decision into a testable seam or assert via a tracked-events spy on `js/contact.js`). Not gated. Parallel-safe (contact.js disjoint).
- [ ] S16 [app-infra] AWS Budget / billing alarm for the events path → a cost-spike on the events pipeline is caught (budget alarm exists; may be account-level / out-of-template per ADR) (inv: SEC-3 budget alarm) — verify: review against ADR-0009 §SEC-3 (config/runbook; cheapest mitigation, ship first within P1 infra). GATED: SECURITY_REVIEW=1 if in aws/template.yaml (ADR-0009); none if account-level. Parallel-safe with S7 (template) — serialize with S7 if both edit template.yaml.
- [x] S17 [chat] deep probe has a server-side cooldown → a second `?deep=1` smoke within a short min-interval is rejected (429/"cooldown") instead of minting a paid Live session; after the interval it is allowed and the once-daily report caller is unaffected (inv: SEC-7/CHAT-SMOKE-1) — target: NEW `docker/chat/tests/test_chat_smoke_cooldown.py` asserting the second back-to-back deep probe is refused and a post-interval one passes. GATED: SECURITY_REVIEW=1 (docker/chat/app/main.py in glob; not cleared by ADR-0008/0009 → flag for navigator, see notes). Parallel-safe with all app/infra slices (chat suite).
- [x] S18 [chat] probe-scoped key distinct from contact-admin key → `/api/chat/smoke` validates a probe-scoped credential (its own chat-container env secret, timing-safe compare); the contact-admin key alone does NOT unlock the smoke endpoint (inv: FE-2 trust-domain split) — target: NEW/extended `docker/chat/tests/test_chat_smoke.py` asserting the new key passes, the old contact-admin key is rejected, and host-status keeps its own gate. GATED: SECURITY_REVIEW=1 (main.py in glob → flag, see notes). Serialize with S17 (same file main.py).
- [x] S19 [fe-manual] admin SPA sends the probe-scoped key + deep probe still works → the admin SPA forwards the probe key (not the contact-admin key) to the chat host and the deep probe works end-to-end against staging (inv: FE-2 UX) — verify: qa-verifier in the admin panel against staging after the split (design-notes Decision 3). Touches js/admin.js (not gated). Depends S18.
- [x] S20 [fe-manual] avgFirstToken labels read correctly in the admin panel → the admin panel shows the canonical/labelled avg so the operator is not shown two conflated numbers (inv: AGG-2 UX) — verify: qa-verifier reads the admin panel. Touches js/admin.js (not gated). Depends S9.

## P2 — follow-ups (this milestone if time; else fast-follow with owner sign-off)

- [x] S21 [app] AGG-1 day-boundary +00:00 chat rows captured → the `:start` lower bound sorts at/below both `...000Z` and `...+00:00` (e.g. fractionless `${day}T00:00:00`), so a `+00:00` midnight chat row is included in the next day's lookback (inv: AGG-1) — target: extend `test/report-queries.test.mjs` asserting a `+00:00` midnight row is in-range. GATED: SECURITY_REVIEW=1 (common/report-queries-core.js in glob → flag, see notes). Parallel-safe.
- [x] S22 [app] EV-1 per-day query is bounded under a flood → `queryDayWith` caps materialized item count OR aggregates incrementally instead of collecting a whole day in memory; existing report-queries tests stay green (inv: EV-1) — target: extend `test/report-queries.test.mjs` pinning the bounded/incremental behavior. GATED: SECURITY_REVIEW=1 (report-queries-core.js in glob → flag, see notes). Serialize with S21 (same file).
- [x] S23 [app-infra] DynamoDB DR policy → `ContactMessagesTable` + `ChatTranscriptsTable` carry `DeletionPolicy: Retain` + `UpdateReplacePolicy: Retain` + `PointInTimeRecoveryEnabled: true`; `SiteEventsTable` carries `Retain` (PITR optional) (inv: INFRA-4) — verify: review against ADR-0009 §INFRA-4. GATED: SECURITY_REVIEW=1 (ADR-0009; aws/template.yaml). Serialize with S7/S16/S24 (same template).
- [x] S24 [app-infra] cron stagger (INFRA-3) → `ContactFailureReportFunction` moves to `cron(30 11 * * ? *)` while `DailyReportFunction` stays `cron(0 12 * * ? *)`, so the two scheduled functions no longer fire at the same UTC minute (inv: INFRA-3) — verify: review against ADR-0009 §INFRA-3. GATED: SECURITY_REVIEW=1 (ADR-0009; aws/template.yaml). Serialize with S7/S16/S23 (same template).
- [x] S25 [app] SEC-5 stale CORS comment corrected → the `js/site-events.js` comment states the true reason (text/plain keeps the POST a CORS-safelisted simple request, no preflight) and no longer claims a wildcard ACAO (inv: SEC-5) — target: a doc/comment assertion or just the edit; pure comment, zero runtime effect. Not gated. Serialize with S4/S14 (same file).
- [x] S26 [app] TC-01 dead utcDayBounds removed or wired in → `utcDayBounds` is deleted with its test (it's unused, returns half-open bounds divergent from the live inclusive-last-ms path) OR consumed by `report-queries-core` so one tested contract ships (inv: TC-01) — target: edit `events-shared.js`/test or wire into report-queries-core. GATED: SECURITY_REVIEW=1 if the wire-in touches report-queries-core.js (glob); delete-from-events-shared not gated → flag, see notes. Serialize with S3/S13 (events-shared) and S21/S22 (report-queries).
- [x] S27 [app] TC-02 FE beacon module test coverage → a node:test stubbing sendBeacon/fetch/sessionStorage asserts buffer flushes at the cap, `recordEvent` stamps `ts` and no-ops on empty name, `flushEvents` no-ops on empty buffer and swaps the buffer before posting, and the text/plain blob type is preserved (inv: TC-02) — target: extend `test/site-events-session.test.mjs` / NEW `test/site-events.test.mjs`. Not gated. Bundle with S14 (same module). Serialize on js/site-events.js.
- [x] S28 [app] TC-03 thin handler wrappers tested → a handler-level test for `getSessionEvents` (inject a fake `queryDay`) asserts the today-UTC default, the `?date` regex guard, and `lookbackDays:1`; ideally `contact-daily-report`'s `fetchDeepChatChecks` timeout/error mapping too (inv: TC-03) — target: NEW `test/session-events-handler.test.mjs`. Not gated (handler glue above the core seam). Parallel-safe.
- [x] S29 [CI] Lambda runtime + CI node major match → the deploy + test workflows' `actions/setup-node` `node-version` is `22` (matching `Globals.Function.Runtime: nodejs22.x`), and the node:test job stays green on 22 (inv: INFRA-6) — verify: edit `.github/workflows/*.yml`, confirm the suite runs on node 22 in CI. Not gated. Parallel-safe; do near the end (CI hygiene, validates the whole suite on the shipping major).
- [ ] S30 [app-infra] SEC-3 per-IP rate limit (DEFERRED-CANDIDATE) → a per-IP control bounds beacon abuse independent of the global stage throttle (WAF RateBasedRule OR a conditional-write token-bucket keyed by ipHash+minute) (inv: SEC-3 deeper) — verify: review against ADR-0009 §SEC-3 (REJECTED WAF/token-bucket for this blast radius). GATED: SECURITY_REVIEW=1 if template/ingress touched. FLAGGED FOR NAVIGATOR: ADR-0009 explicitly rejects this for a personal portfolio; S5/S13/S16 are the P1 floor. Recommend DEFER — do not build unless owner overrides the ADR.

## P0 ordered list

S1 → S2 → S3 → S4 → S5 → S6 → S7  (S4/S5/S6 consent track + S7 template alarm run in parallel with the S1–S3 ipHash track; S6 is qa-manual after S4/S5).

## Notes / ordering risks / navigator flags

- **CI Amplify env-guard (INFRA-1/FE-1, backlog item 1)** is intentionally placed LAST as a CI slice but its CORRECTNESS note is load-bearing: the `amplify.yml` `preBuild` must run `GVP_EXPECTED_ENV=<per-app> node --test test/frontend-api-url-env-guard.test.mjs` with the PER-APP expected env (prod app=prod, staging app=stage) — NOT a hardcoded `prod`. ADR-0009 §INFRA-1 shows `GVP_EXPECTED_ENV=prod` as the prod-app example; the staging Amplify app legitimately serves staging hosts, so hardcoding prod would fail the staging build. dev-ops wires the per-app env in the Amplify console / amplify.yml. This is **S31 [CI]** — see below; I split it out so the per-app resolution is not lost.
- [x] S31 [CI] Amplify build fails closed on a leaked staging API host → a root `amplify.yml` `preBuild` runs `GVP_EXPECTED_ENV=<per-app: prod for main, stage for agent> node --test test/frontend-api-url-env-guard.test.mjs`; a wrong-environment host exits non-zero and aborts the build, a matching tree passes (inv: INFRA-1/FE-1 structural) — verify: amplify.yml present + the guard command uses the per-app env (NOT hardcoded prod) + dry-run reasoning. amplify.yml is OUTSIDE the SECURITY_GLOB (ADR-0009 §INFRA-1) → no SECURITY_REVIEW. This is P0 by backlog priority but is verification-only (no app code) — runnable in parallel with the whole P0 track; flagged here so the per-app env note survives.
- **Gate-flag uncertainty (navigator):** several P1/P2 slices touch SECURITY_GLOB files that the two ADRs do NOT explicitly clear: EV-3 (S10) + EV-4/SEC-4 (S12) on events-ingress-core.js/contact-shared.js, EV-2 (S11) on common/resend.js, SEC-7 (S17)/FE-2 (S18) on docker/chat/app/main.py, AGG-1 (S21)/EV-1 (S22)/TC-01-wire (S26) on report-queries-core.js. ADR-0008/0009 clear ONLY: contact-shared.js hashIp (S1/S2), events-shared.js pepper+cap (S3/S13), and aws/template.yaml (S7/S23/S24). The orchestrator can still proceed under SECURITY_REVIEW=1 for the others (the change is the failing test's spec), but flagging because no ADR records the deliberate review for those surfaces — navigator may want a short clearance note appended to ADR-0009's backlog list (it already names SEC-4, EV-2, AGG-1) or a one-line ADR addendum for SEC-7/FE-2 (chat key split) and EV-3.
- **S30 (per-IP WAF/token-bucket):** recommend DEFER — ADR-0009 explicitly rejects it for this blast radius; the P1 floor (S13 lowered fan-out + S16 budget alarm) ships regardless. Included only so the milestone's P2 list is complete; do not pad the build with it absent an owner override.
- **Parallel vs serialize:** the ipHash track (S1→S2→S3→S13) and the consent/beacon track (S4→S5→S14→S25→S27, all js/site-events.js + js/analytics.js + js/consent.js) and the template track (S7→S16→S23→S24, all aws/template.yaml) and the chat track (S17→S18) and the report-queries track (S21→S22→S26) are four/five independent files that can fan out across cycles but each track MUST serialize internally (same file). Disjoint single-shot slices (S8, S9, S11, S15, S28, S29, S31) can run anytime.
