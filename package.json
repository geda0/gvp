{
  "name": "gvp-portfolio",
  "private": true,
  "description": "Static portfolio site with an AWS durable contact pipeline.",
  "type": "module",
  "scripts": {
    "test:reduced-motion": "node --test test/starfield-reduced-motion.test.mjs",
    "sam:build": "cd aws && sam build --template-file template.yaml"
  }
}
