{
  "name": "gvp-portfolio",
  "private": true,
  "description": "Static portfolio site with an AWS durable contact pipeline.",
  "type": "module",
  "scripts": {
    "test:reduced-motion": "node --test test/starfield-reduced-motion.test.mjs",
    "sam:build": "sam build --template aws/template.yaml",
    "sam:deploy": "sam deploy --guided --template-file aws/template.yaml"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.888.0",
    "@aws-sdk/client-sqs": "^3.888.0",
    "@aws-sdk/lib-dynamodb": "^3.888.0"
  }
}
