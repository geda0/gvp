node_modules
.cursor/

.env
.env.*
!.env.example
!docker/.env.example
aws/.env

# GCP / AWS credentials (never commit)
**/google-sa*.json
**/*service-account*.json
**/*-credentials.json
*.pem

DNS_A
.aws-sam

# OS / editor noise
.DS_Store
**/.DS_Store

# Python bytecode
*.pyc
**/__pycache__/

# Local installers (never commit)
AWSCLIV2.pkg

# Optional local overrides
config.local.js

# Local CI/CD + secrets (never commit): copy from secrets.example/
.secrets/
docker/chat/.venv/
docker/chat/.venv-ci/
docker/chat/.venv-samtest/

# leftover agent-session worktrees (Claude/Cursor snapshots — never commit)
.claude/worktrees/

# >>> team-tactics (managed) >>>
# Transient kit artifacts - never commit (other state files ARE committed for continuity).
.claude/state/suite-status
.claude/state/telemetry.jsonl
.claude/state/tics.jsonl
.claude/state/tics.d/
.claude/**/*.bak
.claude/agents/*.md.kit-*
*.team-tactics.*
# MCP server configs hold machine-specific absolute paths — never commit (re-generate via mcp-install).
.mcp.json
.cursor/mcp.json
# <<< team-tactics (managed) <<<
